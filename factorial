#include <bits/stdc++.h>
using namespace std;

long fact(int n){
    if(n<=1)
        return 1;
    return n*fact(n-1);
}

int main(){
    int n;
    cout<<"enter n: ";
    cin>>n;
    long f=fact(n);
    cout<<"Factorial of "<<n<<" = "<<f<<endl;
}
